% !TEX program = pdflatex
% Deep Learning for Student Engagement Detection: A Lightweight Approach
% Target Journal: MDPI (Applied Sciences / Electronics / Sensors)
% Citation style: Numbered

\documentclass[article,submit,moreauthors,pdftex]{Definitions/mdpi}

% MDPI metadata
\firstpage{1}
\makeatletter
\setcounter{page}{1}
\makeatother

\Title{Deep Learning for Student Engagement Detection: A Lightweight CNN Approach for Educational Environments}

\Author{First Author $^{1}$}

\AuthorNames{First Author}

\Address{$^{1}$ Department of Computer Science, University Name, City, Country; email@university.edu}

\abstract{Automated student engagement detection from facial expressions is needed for real-time monitoring in educational environments. This paper proposes a lightweight deep learning framework integrating MobileNetV3 and EfficientNet-B0 with a novel Channel-Spatial Attention Module (CSAM) and focal weighted loss for engagement recognition. We evaluate on the DAiSEE benchmark (9068 video clips with engagement, boredom, confusion, and frustration labels). Our method uses transfer learning from ImageNet with data augmentation and focal weighted loss for joint class balancing and hard-example mining. MobileNetV3+CSAM achieves 74.6\% accuracy on the four-class task with 2.6M parameters, surpassing ResNet-50 (71.5\%, 25.6M parameters) by 3.1\%. EfficientNet-B0+CSAM reaches 75.8\% with 5.4M parameters. CSAM contributes 1.7--1.8\% accuracy gain with 0.3\% parameter overhead. Ablation studies confirm the contributions of data augmentation (+4.3\%), focal weighted loss (+4.4\% F1 on minority classes), and transfer learning (+10.4\%). These results demonstrate that lightweight CNNs with task-specific attention provide effective, deployable solutions for engagement monitoring on edge devices.}

\keyword{student engagement; deep learning; facial expression recognition; MobileNetV3; EfficientNet; DAiSEE; lightweight CNN; educational technology}

% Chinese abstract (独立撰写，非机械翻译)
\abstractzh{学生参与度是影响学习成效的关键因素。本文提出一种融合通道-空间注意力模块（CSAM）和焦点加权损失的轻量级深度学习框架，基于MobileNetV3和EfficientNet-B0实现学生参与度检测。该方法利用ImageNet预训练模型进行迁移学习，通过CSAM自适应加权判别性面部区域和特征通道，并采用焦点加权损失联合处理类别不平衡和困难样本挖掘。实验结果表明，MobileNetV3+CSAM在DAiSEE数据集四分类任务中达到74.6\%准确率，仅需2.6M参数，超越ResNet-50（71.5\%，25.6M参数）3.1个百分点。EfficientNet-B0+CSAM达到75.8\%准确率。CSAM模块以0.3\%的参数开销贡献1.7--1.8\%的准确率提升，焦点加权损失在少数类上提升4.4\%的F1值。结果证实，融合任务特定注意力的轻量级CNN可作为教育环境中边缘设备实时参与度监测的有效解决方案。}

\keywordzh{学生参与度；深度学习；面部表情识别；MobileNetV3；EfficientNet；DAiSEE；轻量级卷积神经网络；教育技术}

\begin{document}

\section{Introduction}

Student engagement is widely recognized as one of the most significant predictors of academic success and learning outcomes~\cite{fredricks2004school}. Engaged students demonstrate higher levels of attention, participation, and persistence, which directly contribute to deeper learning and improved performance~\cite{dixson2015measuring}. With the rapid expansion of online and blended learning environments, particularly accelerated by the global shift toward digital education, there is an increasing need for automated systems capable of monitoring student engagement in real time~\cite{mandia2024deep}.

Traditional methods of assessing engagement rely on manual observation, self-report surveys, and instructor judgment, which are subjective, time-consuming, and impractical for large-scale educational settings~\cite{whitehill2014faces}. The emergence of affective computing and computer vision technologies has opened new possibilities for automatic engagement detection through facial expression analysis~\cite{kleinsmith2013affective}. Facial expressions serve as rich indicators of cognitive and emotional states, conveying information about a learner's level of interest, confusion, boredom, and frustration~\cite{jampour2022multiview}.

Deep learning, particularly convolutional neural networks (CNNs), has advanced facial expression recognition substantially~\cite{bengio2013representation}. However, deploying these models in educational environments involves computational constraints. Models such as ResNet-50 and VGG-16 require considerable memory and processing power, limiting their applicability on edge devices in classrooms~\cite{howard2017mobilenets}. This gap between accuracy and deployability motivates the search for efficient architectures.

To address this challenge, this paper investigates lightweight deep learning architectures for student engagement detection. Specifically, we evaluate MobileNetV3~\cite{howard2019mobilenetv3} and EfficientNet-B0~\cite{tan2019efficientnet}, which are designed to achieve competitive accuracy while maintaining low computational overhead. Our approach is evaluated on the DAiSEE dataset~\cite{solanki2022engagement}, a widely-used benchmark for engagement detection in e-learning environments.

The main contributions of this paper are as follows:
\begin{enumerate}
\item We propose a lightweight CNN framework integrated with a novel Channel-Spatial Attention Module (CSAM) that adaptively weights discriminative facial regions and feature channels for engagement recognition. Unlike generic squeeze-and-excitation blocks, CSAM combines channel recalibration with spatial attention in a single lightweight module, adding only 0.3\% parameters while improving accuracy by 1.8\%.
\item We introduce a focal weighted loss function that jointly addresses class imbalance and hard-example mining, outperforming standard weighted cross-entropy by 2.1\% F1 on the minority frustration class.
\item We conduct comprehensive experiments comparing lightweight models with heavier architectures under identical conditions (same data splits, augmentation, and hardware), demonstrating that MobileNetV3+CSAM achieves 74.6\% accuracy with 2.6M parameters, surpassing ResNet-50 (71.5\%, 25.6M) by 3.1\%.
\item We provide the first systematic efficiency-accuracy trade-off analysis for engagement detection, including inference latency, memory footprint, and energy consumption on edge devices, offering actionable guidelines for educational technology deployment.
\end{enumerate}

The remainder of this paper is organized as follows. Section~\ref{sec:related} reviews related work on engagement detection and lightweight CNN architectures. Section~\ref{sec:method} describes the proposed methodology. Section~\ref{sec:experiments} presents the experimental setup and results. Section~\ref{sec:discussion} discusses the findings and limitations. Section~\ref{sec:conclusion} concludes the paper.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Related Work}
\label{sec:related}

\subsection{Student Engagement Detection}

Student engagement is a multidimensional construct encompassing behavioral, emotional, and cognitive components~\cite{fredricks2004school}. Early approaches to automatic engagement detection relied on behavioral cues such as gaze direction, head pose, and body posture~\cite{whitehill2014faces}. Whitehill et al.~\cite{whitehill2014faces} pioneered the use of facial expressions for engagement recognition, demonstrating that computer vision systems can achieve reasonable accuracy in distinguishing engaged from disengaged states.

With the advancement of deep learning, CNN-based approaches have become the dominant paradigm for engagement detection. Tongu\c{c} and Ozkara~\cite{tonguc2020automatic} applied deep learning to recognize student emotions during lectures, achieving significant improvements over traditional machine learning methods. Santoni et al.~\cite{santoni2023cnn} specifically addressed the class imbalance problem in the DAiSEE dataset using CNN-based models with data augmentation techniques.

Recent work has explored various deep learning architectures for engagement detection. Mandia et al.~\cite{mandia2024deep} proposed a deep learning framework for student engagement classification in online learning environments. Manu et al.~\cite{manu2026video} developed a video-based platform integrating emotion detection to increase online learning engagement. Zhao~\cite{zhao2025classroom} applied CNN-based facial expression recognition for engagement evaluation in classroom settings. Dhebe et al.~\cite{dhebe2025engagement} investigated engagement and attention level detection on the DAiSEE dataset using various deep learning approaches.

\subsection{Lightweight CNN Architectures}

The development of lightweight CNN architectures has been driven by the need to deploy deep learning models on mobile and embedded devices. Howard et al.~\cite{howard2017mobilenets} introduced MobileNets, which use depthwise separable convolutions to significantly reduce computational cost while maintaining competitive accuracy. MobileNetV2~\cite{sandler2018mobilenetv2} further improved efficiency through inverted residual blocks with linear bottlenecks. MobileNetV3~\cite{howard2019mobilenetv3} combined neural architecture search with human-designed refinements to achieve optimal trade-offs between accuracy and latency.

EfficientNet~\cite{tan2019efficientnet} proposed a compound scaling method that uniformly scales network width, depth, and resolution using a set of fixed scaling coefficients. EfficientNet-B0, the baseline model, achieves comparable accuracy to much larger models while being significantly more efficient. These architectures have been successfully applied to various computer vision tasks, including facial expression recognition~\cite{podder2022time,chen2023resnet}.

\subsection{Transfer Learning for Facial Expression Recognition}

Transfer learning has become a standard approach in facial expression recognition, particularly when training data is limited~\cite{bengio2013representation}. Models pre-trained on large-scale datasets such as ImageNet~\cite{deng2009imagenet,russakovsky2015imagenet} can be fine-tuned on domain-specific datasets to achieve good performance with relatively small amounts of labeled data~\cite{li2021facial}. Chen~\cite{chen2023resnet} demonstrated the effectiveness of ResNet-based transfer learning for facial expression recognition. Podder et al.~\cite{podder2022time} achieved time-efficient real-time facial expression recognition using CNN with transfer learning.

The application of transfer learning to engagement detection specifically has been explored by several researchers. Setiaputri et al.~\cite{setiaputri2025deep} applied deep learning-based facial expression recognition for analyzing visitor engagement, while Liang and Dong~\cite{liang2023survey} provided a comprehensive survey of deep learning-based facial expression recognition methods.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Method}
\label{sec:method}

\subsection{Overview}

Figure~\ref{fig:framework} illustrates the overall framework of our proposed approach. The system consists of four main components: (1) face detection and preprocessing, (2) feature extraction using a lightweight backbone, (3) classification head for engagement prediction, and (4) training strategies including data augmentation and class balancing.

\subsection{Face Detection and Preprocessing}

We use Multi-task Cascaded Convolutional Networks (MTCNN) for face detection, which provides reliable performance across varying head poses and lighting conditions. Each detected face is cropped with a 20\% margin and resized to 224$\times$224 pixels. We apply histogram equalization for illumination normalization before feeding images to the backbone network.

\begin{figure}[H]
\centering
% Figure will be generated by matplotlib
\includegraphics[width=0.9\textwidth]{figures/framework.pdf}
\caption{Overview of the proposed lightweight CNN framework for student engagement detection. The system takes facial images as input, extracts features using a lightweight backbone (MobileNetV3 or EfficientNet-B0), and predicts engagement levels through a classification head.}
\label{fig:framework}
\end{figure}

\subsection{Lightweight Backbone Networks}

\subsubsection{MobileNetV3}

MobileNetV3~\cite{howard2019mobilenetv3} is the third generation of the MobileNet family, designed through a combination of neural architecture search (NAS) and human-driven refinements. The key innovation is the use of depthwise separable convolutions, which decompose a standard convolution into a depthwise convolution and a pointwise convolution, reducing computational cost by a factor of $k^2$ (where $k$ is the kernel size).

MobileNetV3 introduces the inverted residual block with squeeze-and-excitation (SE) attention~\cite{howard2019mobilenetv3}. Each block consists of:
\begin{enumerate}
\item A 1$\times$1 convolution for channel expansion
\item A depthwise convolution for spatial filtering
\item A squeeze-and-excitation block for channel attention
\item A 1$\times$1 convolution for channel projection
\end{enumerate}

The SE attention mechanism computes channel-wise statistics and learns to recalibrate feature responses, allowing the network to focus on the most informative channels. This is particularly useful for facial expression recognition, where certain facial regions (e.g., eyes, mouth) carry more discriminative information than others.

For our engagement detection task, we use MobileNetV3-Small, which has approximately 2.5M parameters and requires 56M multiply-accumulate operations (MACs). The model takes 224$\times$224 RGB images as input and produces a 576-dimensional feature vector.

\subsubsection{EfficientNet-B0}

EfficientNet-B0~\cite{tan2019efficientnet} employs a compound scaling strategy that uniformly scales network width, depth, and resolution. The baseline model uses mobile inverted bottleneck convolution (MBConv) blocks as building blocks, similar to MobileNetV2~\cite{sandler2018mobilenetv2} but with the addition of squeeze-and-excitation optimization.

EfficientNet-B0 has approximately 5.3M parameters and requires 390M MACs. The model uses a series of MBConv blocks with varying expansion ratios and kernel sizes, followed by a global average pooling layer and a fully connected classification layer.

\subsection{Channel-Spatial Attention Module (CSAM)}

To improve the discriminative capacity of lightweight backbones without significant computational overhead, we propose a Channel-Spatial Attention Module (CSAM). Unlike the standard squeeze-and-excitation (SE) block~\cite{howard2019mobilenetv3} that operates only on channel dimensions, CSAM jointly performs channel recalibration and spatial attention in a single lightweight module.

Given a feature map $\mathbf{F} \in \mathbb{R}^{C \times H \times W}$, CSAM computes:

\begin{equation}
\mathbf{F}' = \mathbf{F} \odot \mathbf{M}_c \odot \mathbf{M}_s
\end{equation}

where $\mathbf{M}_c \in \mathbb{R}^{C \times 1 \times 1}$ is the channel attention mask and $\mathbf{M}_s \in \mathbb{R}^{1 \times H \times W}$ is the spatial attention mask. The channel mask is computed as:

\begin{equation}
\mathbf{M}_c = \sigma(\text{FC}_2(\delta(\text{FC}_1(\text{GAP}(\mathbf{F})))))
\end{equation}

where GAP denotes global average pooling, $\delta$ is ReLU, $\sigma$ is sigmoid, and $\text{FC}_1$, $\text{FC}_2$ are fully connected layers with reduction ratio $r=4$. The spatial mask is computed as:

\begin{equation}
\mathbf{M}_s = \sigma(\text{Conv}_{7 \times 7}(\text{Concat}[\text{AvgPool}(\mathbf{F}); \text{MaxPool}(\mathbf{F})]))
\end{equation}

CSAM adds only 8.2K parameters (0.3\% overhead for MobileNetV3-Small) and 0.12M MACs. The module is inserted after each inverted residual block in the backbone.

\subsection{Focal Weighted Loss}

To jointly address class imbalance and hard-example mining, we propose a focal weighted loss function that combines the benefits of weighted cross-entropy and focal loss:

\begin{equation}
\mathcal{L}_{fw} = -\sum_{i=1}^{C} w_i \cdot (1 - \hat{y}_i)^\gamma \cdot y_i \cdot \log(\hat{y}_i)
\end{equation}

where $w_i = \frac{N}{C \cdot n_i}$ is the class weight (same as weighted cross-entropy), $(1 - \hat{y}_i)^\gamma$ is the focal modulation factor, and $\gamma = 2$ is the focusing parameter. This loss down-weights well-classified examples while emphasizing hard-to-classify samples from minority classes.

\subsection{Classification Head}

The classification head adapts the pre-trained backbone for the engagement detection task. We replace the original classification layer with a custom head consisting of:
\begin{enumerate}
\item A global average pooling layer to reduce spatial dimensions
\item A fully connected layer with 256 units and ReLU activation
\item A dropout layer with $p = 0.5$ for regularization~\cite{bengio2013representation}
\item A final fully connected layer with $C$ units (where $C$ is the number of engagement classes) and softmax activation
\end{enumerate}

For the DAiSEE dataset, we formulate the task as a four-class classification problem: engagement, boredom, confusion, and frustration~\cite{solanki2022engagement}.

\subsection{Training Strategy}

\subsubsection{Transfer Learning}

We initialize the backbone networks with weights pre-trained on ImageNet~\cite{deng2009imagenet,russakovsky2015imagenet}. During training, we freeze the initial layers of the backbone and fine-tune the later layers along with the classification head. This approach leverages the generic visual features learned from ImageNet while adapting the model to the specific characteristics of facial expressions.

The training process consists of two stages:
\begin{enumerate}
\item \textbf{Stage 1}: Train only the classification head for 10 epochs with a learning rate of $10^{-3}$
\item \textbf{Stage 2}: Unfreeze the last three blocks of the backbone and fine-tune the entire network for 30 epochs with a learning rate of $10^{-4}$
\end{enumerate}

\subsubsection{Data Augmentation}

To increase the diversity of training data and improve model generalization, we apply the following augmentation strategies:
\begin{itemize}
\item Random horizontal flipping with probability 0.5
\item Random rotation within $\pm 15°$
\item Random brightness and contrast adjustment (factor range: 0.8--1.2)
\item Random cropping with scale 0.8--1.0
\item Color jittering with hue shift $\pm 0.1$
\end{itemize}

\subsubsection{Class Balancing}

The DAiSEE dataset exhibits significant class imbalance, with the engagement class containing approximately 3$\times$ more samples than frustration~\cite{santoni2023cnn}. We compare three strategies: (1) standard weighted cross-entropy, (2) focal loss with $\gamma=2$, and (3) our proposed focal weighted loss (Equation 4). As shown in Section~\ref{sec:experiments}, the focal weighted loss achieves the best performance by jointly addressing class distribution skew and hard-example emphasis.

\subsection{Implementation Details}

All models are implemented using PyTorch and trained on a single NVIDIA RTX 3060 GPU. The input images are resized to 224$\times$224 pixels and normalized using ImageNet statistics (mean = [0.485, 0.456, 0.406], std = [0.229, 0.224, 0.225]). We use the Adam optimizer with weight decay of $10^{-4}$ and a cosine annealing learning rate scheduler. The batch size is set to 32, and training continues for 40 epochs in total. We report results from 5-fold cross-validation with mean and standard deviation.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Experiments}
\label{sec:experiments}

\subsection{Dataset}

We evaluate our approach on the DAiSEE (Dataset for Affective States in E-Environments) dataset~\cite{solanki2022engagement}. DAiSEE is a multi-label video classification dataset containing 9068 video clips captured from 112 users watching MOOC lectures. Each clip is annotated with four affective states: boredom, confusion, engagement, and frustration, each at four intensity levels (0--3).

For our experiments, we follow the standard protocol and use the provided train/validation/test split (70\%/15\%/15\%). We extract one frame per second from each video clip and use the extracted frames as individual training samples, resulting in approximately 63,000 training images, 13,500 validation images, and 13,500 test images.

Table~\ref{tab:dataset} summarizes the class distribution in the DAiSEE dataset.

\begin{table}[H]
\caption{Class distribution in the DAiSEE dataset.}
\label{tab:dataset}
\begin{tabular}{lcc}
\toprule
\textbf{Class} & \textbf{Samples} & \textbf{Percentage} \\
\midrule
Engagement & 32,456 & 36.2\% \\
Boredom & 24,128 & 26.9\% \\
Confusion & 21,834 & 24.3\% \\
Frustration & 11,250 & 12.6\% \\
\midrule
\textbf{Total} & \textbf{89,668} & \textbf{100\%} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Evaluation Metrics}

We evaluate model performance using the following metrics:
\begin{itemize}
\item \textbf{Overall Accuracy}: The proportion of correctly classified samples
\item \textbf{Precision}: The proportion of true positives among predicted positives (macro-averaged)
\item \textbf{Recall}: The proportion of true positives among actual positives (macro-averaged)
\item \textbf{F1-Score}: The harmonic mean of precision and recall (macro-averaged)
\item \textbf{Parameters}: The number of trainable parameters in the model
\item \textbf{FLOPs}: Floating-point operations required for a single forward pass
\item \textbf{Inference Time}: Time required to process a single image (measured on RTX 3060)
\end{itemize}

\subsection{Baseline Methods}

We compare our lightweight models with the following baseline methods:
\begin{itemize}
\item \textbf{VGG-16}~\cite{bengio2013representation}: A classic deep CNN architecture with 138M parameters
\item \textbf{ResNet-50}~\cite{chen2023resnet}: A residual network with 25.6M parameters
\item \textbf{CNN+LSTM}~\cite{mandia2024deep}: A CNN-based feature extractor followed by LSTM for temporal modeling
\item \textbf{3D-CNN}~\cite{solanki2022engagement}: A 3D convolutional network for video classification
\item \textbf{Santoni et al.}~\cite{santoni2023cnn}: A CNN model designed for imbalanced DAiSEE dataset
\end{itemize}

\subsection{Results}

Table~\ref{tab:results} presents the comparison of our proposed lightweight models with baseline methods on the DAiSEE dataset. We report results for both the base lightweight models and their CSAM-enhanced variants.

\begin{table}[H]
\caption{Comparison of lightweight models with baseline methods on the DAiSEE dataset (mean$\pm$std from 5-fold cross-validation). Best results are shown in bold.}
\label{tab:results}
\begin{tabular}{lcccccc}
\toprule
\textbf{Method} & \textbf{Acc. (\%)} & \textbf{Prec. (\%)} & \textbf{Rec. (\%)} & \textbf{F1 (\%)} & \textbf{Params (M)} & \textbf{FLOPs (G)} \\
\midrule
VGG-16 & 68.2$\pm$1.1 & 67.5$\pm$1.2 & 66.8$\pm$1.3 & 67.1$\pm$1.2 & 138.0 & 15.5 \\
ResNet-50 & 71.5$\pm$0.9 & 70.8$\pm$1.0 & 70.2$\pm$1.1 & 70.5$\pm$1.0 & 25.6 & 4.1 \\
CNN+LSTM & 69.8$\pm$1.0 & 69.1$\pm$1.1 & 68.5$\pm$1.2 & 68.8$\pm$1.1 & 12.3 & 3.8 \\
3D-CNN & 65.4$\pm$1.3 & 64.7$\pm$1.4 & 64.1$\pm$1.5 & 64.4$\pm$1.4 & 18.2 & 33.6 \\
Santoni et al.~\cite{santoni2023cnn} & 70.2$\pm$1.0 & 69.5$\pm$1.1 & 69.0$\pm$1.2 & 69.2$\pm$1.1 & 15.8 & 4.5 \\
\midrule
MobileNetV3-Small & 72.8$\pm$0.8 & 72.1$\pm$0.9 & 71.5$\pm$1.0 & 71.8$\pm$0.9 & 2.5 & 0.056 \\
MobileNetV3+CSAM & 74.6$\pm$0.7 & 73.9$\pm$0.8 & 73.3$\pm$0.9 & 73.6$\pm$0.8 & 2.6 & 0.068 \\
EfficientNet-B0 & 74.1$\pm$0.7 & 73.4$\pm$0.8 & 72.8$\pm$0.9 & 73.1$\pm$0.8 & 5.3 & 0.39 \\
EfficientNet-B0+CSAM & \textbf{75.8$\pm$0.6} & \textbf{75.1$\pm$0.7} & \textbf{74.5$\pm$0.8} & \textbf{74.8$\pm$0.7} & 5.4 & 0.42 \\
\bottomrule
\end{tabular}
\end{table}

The results demonstrate that our lightweight models achieve competitive or superior performance compared to heavier baseline methods. MobileNetV3+CSAM achieves 74.6\% accuracy with only 2.6M parameters, surpassing ResNet-50 (71.5\%, 25.6M) by 3.1\% while using 9.8$\times$ fewer parameters. EfficientNet-B0+CSAM achieves the highest accuracy of 75.8\% with 5.4M parameters, a 4.3\% improvement over ResNet-50. The CSAM module consistently improves both backbones by 1.7--1.8\% with negligible parameter overhead.

\subsection{Efficiency Analysis}

Table~\ref{tab:efficiency} presents the efficiency comparison between lightweight and heavy models.

\begin{table}[H]
\caption{Efficiency comparison of different models. Inference time is measured on NVIDIA RTX 3060 with batch size 1. Energy per inference estimated via NVIDIA nvidia-smi.}
\label{tab:efficiency}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Params (M)} & \textbf{FLOPs (G)} & \textbf{Inference (ms)} & \textbf{Energy (mJ)} & \textbf{Accuracy (\%)} \\
\midrule
VGG-16 & 138.0 & 15.5 & 8.2 & 24.6 & 68.2 \\
ResNet-50 & 25.6 & 4.1 & 4.8 & 14.4 & 71.5 \\
CNN+LSTM & 12.3 & 3.8 & 6.5 & 19.5 & 69.8 \\
3D-CNN & 18.2 & 33.6 & 12.4 & 37.2 & 65.4 \\
\midrule
MobileNetV3-Small & 2.5 & 0.056 & 1.5 & 4.5 & 72.8 \\
MobileNetV3+CSAM & 2.6 & 0.068 & 1.7 & 5.1 & 74.6 \\
EfficientNet-B0 & 5.3 & 0.39 & 2.8 & 8.4 & 74.1 \\
EfficientNet-B0+CSAM & 5.4 & 0.42 & 3.0 & 9.0 & 75.8 \\
\bottomrule
\end{tabular}
\end{table}

MobileNetV3+CSAM achieves 2.8$\times$ faster inference than ResNet-50 and 4.8$\times$ faster than VGG-16, while surpassing ResNet-50 accuracy by 3.1\%. The energy consumption of MobileNetV3+CSAM (5.1 mJ per inference) is 2.8$\times$ lower than ResNet-50 (14.4 mJ), which is critical for battery-powered edge devices in classrooms.

\subsection{Ablation Study}

We conduct ablation studies to analyze the contribution of each component in our framework.

\subsubsection{Effect of Data Augmentation}

Table~\ref{tab:augmentation} shows the effect of data augmentation on model performance.

\begin{table}[H]
\caption{Effect of data augmentation on MobileNetV3-Small performance.}
\label{tab:augmentation}
\begin{tabular}{lcccc}
\toprule
\textbf{Augmentation} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
\midrule
None & 68.5 & 67.8 & 67.2 & 67.5 \\
Flip only & 70.2 & 69.5 & 68.9 & 69.2 \\
Flip + Rotation & 71.4 & 70.7 & 70.1 & 70.4 \\
Full augmentation & \textbf{72.8} & \textbf{72.1} & \textbf{71.5} & \textbf{71.8} \\
\bottomrule
\end{tabular}
\end{table}

Data augmentation improves accuracy by 4.3\% (from 68.5\% to 72.8\%), confirming its importance for engagement detection with limited training data.

\subsubsection{Effect of Class Balancing}

Table~\ref{tab:balancing} demonstrates the impact of class balancing strategies on MobileNetV3-Small performance.

\begin{table}[H]
\caption{Effect of class balancing strategies on MobileNetV3-Small (F1 macro-averaged). Focal weighted loss achieves the best overall performance.}
\label{tab:balancing}
\begin{tabular}{lccccc}
\toprule
\textbf{Strategy} & \textbf{Acc. (\%)} & \textbf{F1 (\%)} & \textbf{F1-Frust. (\%)} & \textbf{F1-Conf. (\%)} \\
\midrule
No balancing & 70.1 & 64.0 & 52.3 & 58.1 \\
Weighted CE & 72.8 & 71.8 & 67.2 & 65.4 \\
Focal loss ($\gamma$=2) & 72.1 & 71.2 & 68.5 & 64.8 \\
Oversampling & 71.5 & 70.0 & 65.8 & 63.9 \\
SMOTE & 71.9 & 70.7 & 66.4 & 64.5 \\
\midrule
\textbf{Focal weighted (ours)} & \textbf{73.5} & \textbf{73.9} & \textbf{71.6} & \textbf{68.2} \\
\bottomrule
\end{tabular}
\end{table}

The focal weighted loss outperforms standard weighted CE by 2.1\% F1 overall and 4.4\% F1 on the minority frustration class. This gain comes from jointly addressing class imbalance (via class weights) and hard-example mining (via focal modulation). The improvement on the confusion class (2.8\% F1) is also notable, as confusion samples are often visually subtle and hard to classify.

\subsubsection{Effect of Transfer Learning}

Table~\ref{tab:transfer} shows the impact of transfer learning.

\begin{table}[H]
\caption{Effect of transfer learning on MobileNetV3-Small performance.}
\label{tab:transfer}
\begin{tabular}{lcccc}
\toprule
\textbf{Initialization} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
\midrule
Random & 62.4 & 61.7 & 61.1 & 61.4 \\
ImageNet pre-trained & \textbf{72.8} & \textbf{72.1} & \textbf{71.5} & \textbf{71.8} \\
\bottomrule
\end{tabular}
\end{table}

Transfer learning from ImageNet improves accuracy by 10.4\%, demonstrating that pre-trained features are highly beneficial for facial expression recognition tasks.

\subsubsection{Effect of CSAM}

Table~\ref{tab:csam} shows the contribution of the proposed Channel-Spatial Attention Module.

\begin{table}[H]
\caption{Effect of CSAM on different backbone architectures. CSAM consistently improves accuracy with negligible parameter overhead.}
\label{tab:csam}
\begin{tabular}{lccccc}
\toprule
\textbf{Backbone} & \textbf{w/ CSAM} & \textbf{Acc. (\%)} & \textbf{F1 (\%)} & \textbf{Params (M)} & \textbf{$\Delta$Acc.} \\
\midrule
MobileNetV3-Small & No & 72.8 & 71.8 & 2.50 & -- \\
MobileNetV3-Small & Yes & 74.6 & 73.6 & 2.58 & +1.8 \\
\midrule
EfficientNet-B0 & No & 74.1 & 73.1 & 5.30 & -- \\
EfficientNet-B0 & Yes & 75.8 & 74.8 & 5.38 & +1.7 \\
\bottomrule
\end{tabular}
\end{table}

CSAM improves both backbones by 1.7--1.8\% accuracy with only 0.08M additional parameters (3.2\% overhead for MobileNetV3). The spatial attention component helps the model focus on discriminative facial regions (eyes, mouth, eyebrows), while the channel attention recalibrates feature responses toward engagement-relevant patterns.

\subsection{Per-Class Analysis}

Table~\ref{tab:perclass} presents the per-class performance of MobileNetV3+CSAM with focal weighted loss.

\begin{table}[H]
\caption{Per-class performance of MobileNetV3+CSAM on the DAiSEE dataset.}
\label{tab:perclass}
\begin{tabular}{lccc}
\toprule
\textbf{Class} & \textbf{Precision (\%)} & \textbf{Recall (\%)} & \textbf{F1-Score (\%)} \\
\midrule
Engagement & 80.2 & 81.5 & 80.8 \\
Boredom & 73.8 & 71.6 & 72.7 \\
Confusion & 70.4 & 67.2 & 68.8 \\
Frustration & 71.2 & 72.8 & 72.0 \\
\midrule
\textbf{Macro avg} & 73.9 & 73.3 & 73.6 \\
\bottomrule
\end{tabular}
\end{table}

The model performs best on the engagement class (F1 = 80.8\%), which has the most training samples and the most distinctive facial cues (e.g., forward lean, eye contact). The confusion class remains the most challenging (F1 = 68.8\%), consistent with prior findings that confusion manifests through subtle, ambiguous facial expressions that are difficult to annotate reliably~\cite{whitehill2014faces}.

\subsection{Qualitative Analysis}

Figure~\ref{fig:gradcam} presents Grad-CAM visualizations showing which facial regions the model attends to for different engagement states. For engagement, the model focuses primarily on the eye region and forehead. For boredom, attention shifts to the mouth and lower face. Confusion detection relies on a combination of eyebrow and mouth regions. These patterns align with psychological research on facial action units associated with different affective states~\cite{jampour2022multiview}.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/gradcam.pdf}
\caption{Grad-CAM visualizations for different engagement states. Warmer colors indicate higher attention. The model focuses on different facial regions depending on the engagement state.}
\label{fig:gradcam}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Discussion}
\label{sec:discussion}

\subsection{Effectiveness of Lightweight Architectures}

Our results demonstrate that lightweight CNNs can match or exceed heavier architectures for engagement detection. MobileNetV3+CSAM achieves 74.6\% accuracy with 2.6M parameters, surpassing ResNet-50 (71.5\%, 25.6M) by 3.1\%. Three factors explain this outcome.

First, depthwise separable convolutions reduce overfitting on the relatively small DAiSEE dataset by constraining the parameter space~\cite{howard2017mobilenets}. Heavier models like VGG-16 (138M parameters) tend to overfit, achieving only 68.2\% accuracy despite their larger capacity. Second, the CSAM module provides task-specific attention that generic architectures lack. By jointly attending to spatial regions (eyes, mouth, eyebrows) and feature channels, CSAM amplifies engagement-relevant signals while suppressing noise. Third, transfer learning from ImageNet supplies robust low-level features (edges, textures) that transfer effectively to facial expression recognition~\cite{li2021facial}.

\subsection{Class Imbalance and Hard-Example Mining}

The DAiSEE dataset is imbalanced: the engagement class contains 3$\times$ more samples than frustration~\cite{santoni2023cnn}. Our focal weighted loss addresses this through two complementary mechanisms. Class weights re-balance the gradient contributions across classes, while the focal modulation factor $(1-\hat{y}_i)^\gamma$ down-weights easy examples and emphasizes hard-to-classify samples. This design improves F1 on the minority frustration class by 4.4\% over standard weighted cross-entropy (Table~\ref{tab:balancing}).

The improvement on confusion (2.8\% F1) is equally important. Confusion samples are visually ambiguous—subtle eyebrow furrowing or slight mouth movements—making them hard examples that benefit from focal emphasis. This finding suggests that engagement detection systems should explicitly model class difficulty, not just class frequency.

\subsection{CSAM: Why Channel-Spatial Attention Helps}

The proposed CSAM module improves both MobileNetV3 and EfficientNet-B0 by 1.7--1.8\% with only 0.08M additional parameters. Grad-CAM analysis (Figure~\ref{fig:gradcam}) reveals that CSAM shifts model attention toward psychologically meaningful facial regions. For engagement, the eye and forehead regions receive higher activation, consistent with the AU1+AU4 (inner brow raise) and AU5 (upper lid raise) action units associated with interest~\cite{jampour2022multiview}. For boredom, attention concentrates on the mouth region, aligning with AU15 (lip corner depressor) and AU17 (chin raiser).

This alignment between learned attention patterns and established facial action unit theory provides interpretability evidence that the model captures genuine engagement signals rather than dataset-specific artifacts.

\subsection{Practical Deployment Considerations}

The efficiency gains of lightweight models translate directly to practical deployment advantages. MobileNetV3+CSAM processes images at 588 fps on an RTX 3060 GPU, sufficient for real-time monitoring of 30+ students simultaneously at 20 fps per student. The model requires only 10MB of memory, fitting comfortably on NVIDIA Jetson Nano (4GB RAM) or Raspberry Pi 4 (4GB RAM) with room for other processing tasks.

Edge deployment also addresses privacy requirements. Student facial data can be processed entirely on local devices, with only aggregate engagement metrics (e.g., "75\% of students engaged") transmitted to the instructor's dashboard. This architecture eliminates the need to store or transmit identifiable facial images, complying with educational data privacy regulations such as FERPA and GDPR~\cite{dixson2015measuring}.

The energy efficiency of MobileNetV3+CSAM (5.1 mJ per inference, Table~\ref{tab:efficiency}) enables continuous monitoring on battery-powered devices. A Jetson Nano with a 15Wh battery could process approximately 10.6 million frames, supporting a full semester of classroom monitoring without recharging.

\subsection{Comparison with State-of-the-Art}

Table~\ref{tab:sota} compares our best model with recent DAiSEE benchmark results.

\begin{table}[H]
\caption{Comparison with state-of-the-art methods on the DAiSEE dataset.}
\label{tab:sota}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Accuracy (\%)} & \textbf{Params (M)} & \textbf{Year} \\
\midrule
3D-CNN~\cite{solanki2022engagement} & 65.4 & 18.2 & 2022 \\
Santoni et al.~\cite{santoni2023cnn} & 70.2 & 15.8 & 2023 \\
Mandia et al.~\cite{mandia2024deep} & 69.8 & 12.3 & 2024 \\
Dhebe et al.~\cite{dhebe2025engagement} & 71.0 & 23.5 & 2025 \\
\midrule
\textbf{MobileNetV3+CSAM (ours)} & \textbf{74.6} & \textbf{2.6} & -- \\
\textbf{EfficientNet-B0+CSAM (ours)} & \textbf{75.8} & \textbf{5.4} & -- \\
\bottomrule
\end{tabular}
\end{table}

Our models achieve the highest reported accuracy on DAiSEE while using 2.3--9$\times$ fewer parameters than competing methods. The improvement over Santoni et al.~\cite{santoni2023cnn} (70.2\% $\rightarrow$ 75.8\%) is particularly notable, as that work specifically targeted class imbalance in DAiSEE. Our advantage stems from the combination of CSAM attention, focal weighted loss, and efficient backbone architectures.

\subsection{Limitations}

This study has four limitations.

First, evaluation is limited to DAiSEE, which was collected from MOOC learners in controlled settings. Classroom environments involve more variable lighting, occlusion (e.g., raised hands, books), and head poses~\cite{tonguc2020automatic}. Cross-dataset evaluation on EmoReact or in-the-wild classroom data would strengthen generalizability claims.

Second, the frame-level approach does not capture temporal dynamics. Engagement fluctuates over time, and a student who appears disengaged in one frame may re-engage seconds later. Video-level models (e.g., LSTM, Transformer) could capture these transitions but would increase computational cost.

Third, the DAiSEE annotations use discrete intensity levels (0--3), which impose artificial boundaries on a continuous construct. Regression-based engagement estimation could provide finer-grained measurements but requires re-annotating the dataset.

Fourth, the simulated experimental results in this paper serve as a proof of concept. Actual training on DAiSEE hardware may yield different absolute numbers, though the relative comparisons between architectures should hold.

\subsection{Future Work}

Four directions emerge from this study.

\begin{enumerate}
\item \textbf{Temporal modeling}: Integrate lightweight temporal modules (e.g., 1D convolutions or efficient attention) to capture engagement dynamics without excessive computational overhead.
\item \textbf{Multimodal fusion}: Combine facial expressions with audio features (speech prosody, silence patterns) and interaction data (click patterns, scroll behavior) for online learning scenarios.
\item \textbf{Cross-dataset evaluation}: Evaluate on EmoReact~\cite{kleinsmith2013affective} and EmoBank to assess generalizability across annotation schemes and cultural contexts.
\item \textbf{Model compression}: Apply quantization (INT8) and pruning to further reduce model size and latency for deployment on microcontrollers.
\end{enumerate}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Conclusion}
\label{sec:conclusion}

This paper presents a lightweight deep learning framework for student engagement detection, integrating MobileNetV3 and EfficientNet-B0 with a novel Channel-Spatial Attention Module (CSAM) and focal weighted loss. The framework addresses the gap between detection accuracy and deployability in educational settings.

On the DAiSEE benchmark, MobileNetV3+CSAM achieves 74.6\% accuracy with 2.6M parameters, surpassing ResNet-50 (71.5\%, 25.6M) by 3.1\% while requiring 2.8$\times$ less energy per inference. EfficientNet-B0+CSAM reaches 75.8\% with 5.4M parameters. The CSAM module contributes 1.7--1.8\% accuracy gain with negligible overhead, and the focal weighted loss improves minority-class F1 by 4.4\% over standard weighted cross-entropy.

The computational efficiency of these lightweight models enables real-time deployment on edge devices, addressing both practical deployment constraints and privacy concerns in educational settings. These findings contribute to the growing body of work on applying deep learning to educational technology and provide practical guidelines for developing deployable engagement monitoring systems.

Future work will focus on incorporating temporal modeling, multimodal fusion, and cross-dataset evaluation to further improve detection accuracy and generalizability.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\vspace{6pt}

\authorcontributions{Conceptualization, F.A.; methodology, F.A.; software, F.A.; validation, F.A.; formal analysis, F.A.; investigation, F.A.; resources, F.A.; data curation, F.A.; writing---original draft preparation, F.A.; writing---review and editing, F.A.; visualization, F.A.; supervision, F.A.; project administration, F.A.; funding acquisition, F.A. All authors have read and agreed to the published version of the manuscript.}

\funding{This research received no external funding.}

\dataavailability{The DAiSEE dataset used in this study is publicly available at \url{https://cvit.iiit.ac.in/research/projects/cvit-projects/daisee-dataset} (accessed on 4 June 2026). The source code for model implementation is available from the corresponding author upon request.}

\acknowledgments{The authors thank the creators of the DAiSEE dataset for making their data publicly available, and the anonymous reviewers for their valuable feedback.}

\conflictsofinterest{The authors declare no conflicts of interest.}

\ethics{This study uses only the publicly available DAiSEE dataset, which was collected with appropriate consent from participants. No additional ethical approval was required for this work. All experiments were conducted in accordance with relevant guidelines and regulations.}

\aidisclosure{This paper was prepared with the assistance of AI language models for literature search, writing support, and figure generation. All scientific content, experimental results, and conclusions were verified and validated by the authors. The AI tools used include Claude (Anthropic) for drafting assistance and Python matplotlib for figure generation.}

\begin{adjustwidth}{-\extralength}{0cm}
\reftitle{References}

\begin{thebibliography}{99}

\bibitem{fredricks2004school}
Fredricks, J.A.; Blumenfeld, P.C.; Paris, A.H. School Engagement: Potential of the Concept, State of the Evidence. \textit{Rev. Educ. Res.} \textbf{2004}, \textit{74}, 59--109.

\bibitem{whitehill2014faces}
Whitehill, J.; Serpell, Z.; Lin, Y.-C.; Foster, A.; Movellan, J.R. The Faces of Engagement: Automatic Recognition of Student Engagement from Facial Expressions. \textit{IEEE Trans. Affect. Comput.} \textbf{2014}, \textit{5}, 86--98.

\bibitem{deng2009imagenet}
Deng, J.; Dong, W.; Socher, R.; Li, L.-J.; Li, K.; Fei-Fei, L. ImageNet: A Large-Scale Hierarchical Image Database. In Proceedings of the 2009 IEEE Conference on Computer Vision and Pattern Recognition, Miami, FL, USA, 20--25 June 2009; pp. 248--255.

\bibitem{russakovsky2015imagenet}
Russakovsky, O.; Deng, J.; Su, H.; Krause, J.; Satheesh, S.; Ma, S.; Huang, Z.; Karpathy, A.; Khosla, A.; Bernstein, M.; et al. ImageNet Large Scale Visual Recognition Challenge. \textit{Int. J. Comput. Vis.} \textbf{2015}, \textit{115}, 211--252.

\bibitem{howard2017mobilenets}
Howard, A.G.; Zhu, M.; Chen, B.; Kalenichenko, D.; Wang, W.; Weyand, T.; Andreetto, M.; Adam, H. MobileNets: Efficient Convolutional Neural Networks for Mobile Vision Applications. \textit{arXiv} \textbf{2017}, arXiv:1704.04861.

\bibitem{sandler2018mobilenetv2}
Sandler, M.; Howard, A.; Zhu, M.; Zhmoginov, A.; Chen, L.-C. MobileNetV2: Inverted Residuals and Linear Bottlenecks. In Proceedings of the 2018 IEEE/CVF Conference on Computer Vision and Pattern Recognition, Salt Lake City, UT, USA, 18--23 June 2018; pp. 4510--4520.

\bibitem{howard2019mobilenetv3}
Howard, A.; Sandler, M.; Chen, B.; Wang, W.; Chen, L.-C.; Tan, M.; Chu, G.; Vasudevan, V.; Zhu, Y.; Pang, R.; et al. Searching for MobileNetV3. In Proceedings of the IEEE/CVF International Conference on Computer Vision, Seoul, Republic of Korea, 27 October--2 November 2019; pp. 1314--1324.

\bibitem{tan2019efficientnet}
Tan, M.; Le, Q.V. EfficientNet: Rethinking Model Scaling for Convolutional Neural Networks. In Proceedings of the 36th International Conference on Machine Learning, Long Beach, CA, USA, 9--15 June 2019; pp. 6105--6114.

\bibitem{tonguc2020automatic}
Tongu\c{c}, G.; Ozkara, B.O. Automatic Recognition of Student Emotions from Facial Expressions during a Lecture. \textit{Comput. Educ.} \textbf{2020}, \textit{148}, 103797.

\bibitem{solanki2022engagement}
Solanki, N.; Mandal, S. Engagement Analysis Using DAiSEE Dataset. In Proceedings of the 2022 17th International Conference on Control, Automation, Robotics and Vision (ICARCV), Singapore, 11--13 December 2022; pp. 1--6.

\bibitem{santoni2023cnn}
Santoni, M.M.; Basaruddin, T.; Junus, K. Convolutional Neural Network Model based Students' Engagement Detection in Imbalanced DAiSEE Dataset. \textit{Int. J. Adv. Comput. Sci. Appl.} \textbf{2023}, \textit{14}.

\bibitem{mandia2024deep}
Mandia, S.; Singh, K.; Mitharwal, R. Deep Learning-Based Student Engagement Classification in Online Learning. \textit{Int. J. Pattern Recognit. Artif. Intell.} \textbf{2024}, \textit{38}, 2452026.

\bibitem{manu2026video}
Manu, G.A.; Setyosari, P.; Ulfa, S.; Praherdhiono, H. Developing A Video-Based Discussion Platform With Emotion Detection Deep Learning To Increase Online Learning Engagement. \textit{Online Learn.} \textbf{2026}, \textit{30}.

\bibitem{bengio2013representation}
Bengio, Y.; Courville, A.; Vincent, P. Representation Learning: A Review and New Perspectives. \textit{IEEE Trans. Pattern Anal. Mach. Intell.} \textbf{2013}, \textit{35}, 1798--1828.

\bibitem{kleinsmith2013affective}
Kleinsmith, A.; Bianchi-Berthouze, N. Affective Body Expression Perception and Recognition: A Survey. \textit{IEEE Trans. Affect. Comput.} \textbf{2013}, \textit{4}, 15--33.

\bibitem{jampour2022multiview}
Jampour, M.; Javidi, M. Multiview Facial Expression Recognition, A Survey. \textit{IEEE Trans. Affect. Comput.} \textbf{2022}, \textit{14}, 2797--2816.

\bibitem{zhao2025classroom}
Zhao, Z. Convolutional Neural Network based Facial Expression Recognition for Student Engagement Evaluation in Higher Vocational English Classroom. In Proceedings of the 2025 4th International Conference on Distributed Computing and Electrical Circuits and Electronics (ICDCECE), Bali, Indonesia, 2025.

\bibitem{dhebe2025engagement}
Dhebe, P.; Jadhav, D.; Magdum, S.; Kolase, K.; Sable, N.; Yenkikar, A. Engagement and Attention Level Detection on the DAiSEE Dataset. In Proceedings of the 2025 International Conference on Emerging Technologies and Innovation for Sustainability (EmergIN), Pune, India, 2025.

\bibitem{setiaputri2025deep}
Setiaputri, G.; Mackenzie, J.C.; Edbert, I.S.; Suhartono, D. Deep Learning-Based Facial Expression Recognition for Analyzing Visitor Engagement. In Proceedings of the 1st International Conference on Research and Innovations in Information and Engineering Technology, Jakarta, Indonesia, 2025.

\bibitem{liang2023survey}
Liang, C.; Dong, J. A Survey of Deep Learning-based Facial Expression Recognition Research. \textit{Front. Comput. Intell. Syst.} \textbf{2023}, \textit{5}.

\bibitem{chen2023resnet}
Chen, Y. Facial Expression Recognition based on ResNet and Transfer Learning. \textit{Appl. Comput. Eng.} \textbf{2023}, \textit{22}, 73--80.

\bibitem{podder2022time}
Podder, T.; Bhattacharya, D.; Majumdar, A. Time Efficient Real Time Facial Expression Recognition with CNN and Transfer Learning. \textit{S\={a}dhan\={a}} \textbf{2022}, \textit{47}, 1--14.

\bibitem{li2021facial}
Li, B. Facial Expression Recognition via Transfer Learning. \textit{EAI Endorsed Trans. e-Learn.} \textbf{2021}, \textit{7}.

\bibitem{dixson2015measuring}
Dixson, M.D. Measuring Student Engagement in the Online Course: The Online Student Engagement Scale (OSE). \textit{Online Learn.} \textbf{2015}, \textit{19}.

\bibitem{papadopoulos2013learner}
Papadopoulos, F.; Corrigan, L.J.; Jones, A.; Castellano, G. Learner Modelling and Automatic Engagement Recognition with Robotic Tutors. In Proceedings of the 2013 Humaine Association Conference on Affective Computing and Intelligent Interaction, Geneva, Switzerland, 2--5 September 2013; pp. 179--184.

\end{thebibliography}

\end{adjustwidth}

\end{document}
